db:
  image: postgres:9.4
  ports:
    - "5432:5432"
web:
  build: .
  command: bundle exec rails server --port=3000 --binding=0.0.0.0
  volumes:
    - .:/app
  ports:
    - "3000:3000"
  links:
    - db
